{% extends 'base.html.twig' %}

{% block title 'Agence Immobilère' %}

{% block body %}
    <div class="jumbotron text-center">
        <div class="container">
            <h1>Agence Immobilère</h1>
            <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Laboriosam laudantium reiciendis ullam voluptas voluptatem. Aliquam commodi cupiditate distinctio facere nulla perferendis sed sequi. Beatae consequatur eveniet iusto magnam quae saepe.</p>
        </div>
    </div>

    <div class="container">
        <h2>Les derniers biens</h2>
        <div class="row flex">
            {% for property in properties %}
            <div class="col-3">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">
                            <a href="{{ path('property.show', {id: property.id, slug: property.slug}) }}">{{ property.title }}</a>
                        </h5>
                        <p class="card-text">
                            {{ property.city }}({{ property.postalcode }})
                        </p>
                        <div class="text-primary" style="font-weight: bold;font-size: 2rem;">
                            {{ property.FormatedPrice}} €</div>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>

    </div>

{% endblock %}